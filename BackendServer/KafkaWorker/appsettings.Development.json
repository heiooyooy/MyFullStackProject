{
  "Logging": {
    "LogLevel": {
      "Default": "Information", // 可以把默认级别也调低
      "Microsoft.Hosting.Lifetime": "Information",
      "Microsoft.AspNetCore": "Information" // <-- 这是关键！确保添加或修改这一行
    }
  }
}